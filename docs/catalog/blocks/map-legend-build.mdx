---
title: "Archival Map - Legend Build"
description: "Public-domain archival animated-map footage via Moving Image Archive Animated Maps (https://www.movingimagearchive.com/collection/animated-maps). Credit: Moving Image Archive, Trading Centers of the Pacific Coast, 1947. Acquired via /api/clips/36a29a94-bb07-5bf2-ac21-1ad9dab65f76/download; self-hosted seek-safe video block. Not affiliated with Moving Image Archive."
mode: "frame"
---

import { CatalogDetail, CatalogSlot } from "/snippets/catalog-detail.jsx";
import { InstallCommand } from "/snippets/install-command.jsx";

<CatalogDetail
  previewSrc="/public/catalog/blocks/map-legend-build.json"
  compositionId="map-legend-build"
  compositionSrc="compositions/map-legend-build.html"
  title="Archival Map - Legend Build"
  description="Public-domain archival animated-map footage via Moving Image Archive Animated Maps (https://www.movingimagearchive.com/collection/animated-maps). Credit: Moving Image Archive, Trading Centers of the Pacific Coast, 1947. Acquired via /api/clips/36a29a94-bb07-5bf2-ac21-1ad9dab65f76/download; self-hosted seek-safe video block. Not affiliated with Moving Image Archive."
  variables={[{"id":"title","type":"string","label":"Title","default":"Archival Map - Legend Build"},{"id":"credit","type":"string","label":"Credit line","default":"Moving Image Archive, Trading Centers of the Pacific Coast, 1947"}]}
  meta={{"duration":12.01,"width":1920,"height":1080,"category":"Blocks","badge":"Stable","codeLines":143}}
  attribution={{"author":"HeyGen","authorUrl":"https://hyperframes.heygen.com","path":"registry/blocks/map-legend-build","tags":["map","geography","animation","archival","footage","mia","newsreel","video"]}}
  hasCode
  rawUrl="https://raw.githubusercontent.com/heygen-com/hyperframes/main/registry/blocks/map-legend-build/map-legend-build.html"
>

<CatalogSlot slot="code">

```html map-legend-build.html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=1920, height=1080" />
    <script src="https://cdn.jsdelivr.net/npm/gsap@3.14.2/dist/gsap.min.js"></script>
    <style>
      *,
      *::before,
      *::after {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        background: #0a0a0a;
        overflow: hidden;
      }
      #root-map-legend-build {
        position: relative;
        width: 1920px;
        height: 1080px;
        overflow: hidden;
        background: #0a0a0a;
        font-family: "Helvetica Neue", Helvetica, sans-serif;
      }
      #lb-video {
        position: absolute;
        inset: 0;
        width: 100%;
        height: 100%;
        object-fit: contain;
        background: #000;
      }
      #lb-frame {
        position: absolute;
        inset: 36px;
        border: 1px solid rgba(232, 220, 196, 0.28);
        pointer-events: none;
      }
      #lb-title {
        position: absolute;
        left: 72px;
        top: 56px;
        max-width: 1400px;
        color: rgba(245, 236, 214, 0.95);
        font-size: 36px;
        font-weight: 700;
        letter-spacing: 0.14em;
        text-transform: uppercase;
        text-shadow: 0 2px 12px rgba(0, 0, 0, 0.75);
      }
      #lb-credit {
        position: absolute;
        left: 72px;
        right: 72px;
        bottom: 56px;
        color: rgba(232, 220, 196, 0.88);
        font-size: 18px;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        text-shadow: 0 2px 10px rgba(0, 0, 0, 0.8);
      }
      #lb-credit-url {
        display: block;
        margin-top: 8px;
        font-size: 14px;
        letter-spacing: 0.04em;
        text-transform: none;
        color: rgba(196, 181, 154, 0.7);
      }
    </style>
  </head>
  <body>
    <div
      id="root-map-legend-build"
      data-composition-id="map-legend-build"
      data-start="0"
      data-duration="12.01"
      data-width="1920"
      data-height="1080"
    >
      <video
        id="lb-video"
        src="assets/clip.mp4"
        muted
        playsinline
        preload="auto"
        data-start="0"
        data-duration="12.01"
        data-media-start="0"
      ></video>
      <div id="lb-frame" aria-hidden="true"></div>
      <div id="lb-title" data-var-text="title">Archival Map - Legend Build</div>
      <div id="lb-credit">
        <span data-var-text="credit"
          >Footage: Moving Image Archive | Trading Centers of the Pacific Coast</span
        >
        <span id="lb-credit-url"
          >movingimagearchive.com/collection/animated-maps - public domain / free to reuse</span
        >
      </div>
    </div>
    <script>
      (function () {
        window.__timelines = window.__timelines || {};

        var vars =
          window.__hyperframes && window.__hyperframes.getVariables
            ? window.__hyperframes.getVariables()
            : {};

        var title =
          typeof vars.title === "string" && vars.title ? vars.title : "Archival Map - Legend Build";
        var credit =
          typeof vars.credit === "string" && vars.credit
            ? vars.credit
            : "Moving Image Archive, Trading Centers of the Pacific Coast, 1947";

        document.getElementById("lb-title").textContent = title;
        document.querySelector('#lb-credit [data-var-text="credit"]').textContent = credit;

        var tl = gsap.timeline({ paused: true });
        // Credit/title hold - HyperFrames media runtime seeks the video via data-start
        tl.fromTo(
          "#lb-title",
          { opacity: 0 },
          { opacity: 1, duration: 0.6, ease: "power1.out" },
          0,
        );
        tl.fromTo(
          "#lb-credit",
          { opacity: 0 },
          { opacity: 1, duration: 0.6, ease: "power1.out" },
          0.15,
        );
        tl.to({}, { duration: Math.max(0.01, 12.01 - 0.75) });

        window.__timelines["map-legend-build"] = tl;
      })();
    </script>
  </body>
</html>
```

</CatalogSlot>

<CatalogSlot slot="install">

<InstallCommand command="npx hyperframes add map-legend-build" item="map-legend-build" />

That writes `compositions/map-legend-build.html`, plus 2 supporting files under `assets/`.

</CatalogSlot>

<CatalogSlot slot="docs">

## Add it to your video

It runs for 12.01 seconds at 1920×1080. Paste this into your composition:

```html index.html
<div
  data-composition-id="map-legend-build"
  data-composition-src="compositions/map-legend-build.html"
  data-start="0"
  data-duration="12.01"
  data-track-index="1"
  data-width="1920"
  data-height="1080"
></div>
```

Move it in time with `data-start`. Put it on a different timeline row with
`data-track-index`. See [data attributes](/concepts/data-attributes) for the rest.

## Change how it looks

Set these CSS variables on the block:

- `title` - Title. Defaults to `Archival Map - Legend Build`.
- `credit` - Credit line. Defaults to `Moving Image Archive, Trading Centers of the Pacific Coast, 1947`.

## Variables

Every one of these has a default, so the piece works untouched. Set the ones you
want to change on the element:

| Variable | Default | Accepts | What it does |
| --- | --- | --- | --- |
| `title` | `Archival Map - Legend Build` | string |  |
| `credit` | `Moving Image Archive, Trading Centers of the Pacific Coast, 1947` | string |  |

Set them with `data-variable-values` on the element that mounts it. These are the
defaults, so this behaves exactly like the preview above until you change one:

```html wrap
<div
  data-composition-id="map-legend-build"
  data-composition-src="compositions/map-legend-build.html"
  data-variable-values='{"title":"Archival Map - Legend Build","credit":"Moving Image Archive, Trading Centers of the Pacific Coast, 1947"}'
></div>
```

{/* hf:generated-footer */}

Tagged `map` `geography` `animation` `archival` `footage` `mia` `newsreel` `video`.

Created by [HeyGen](https://hyperframes.heygen.com).

## Related topics

- [Browse the complete Catalog](/catalog)
- [Add assets and Catalog items in Studio](/studio/assets-and-blocks)
- [Build a richer composition](/go-further)

</CatalogSlot>

</CatalogDetail>
